## 1. Console 文档入口

- [x] 1.1 在共享 `ConsoleHeader` 中增加文档入口，并让 `ConsoleApp` 传入所需的 i18n 文案与目标路径
- [x] 1.2 将文档入口接到现有 `/usage-doc` 页面，确保管理员和允许访问 Web UI 的普通用户都能从当前 console 前端进入文档

## 2. 验证与回归保护

- [x] 2.1 为共享 console 外壳补测试，验证文档入口存在且目标路径正确
- [x] 2.2 运行与本次改动相关的测试，确认文档入口不会因 console 外壳调整而再次丢失
